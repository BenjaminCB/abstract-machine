import * as x from "module1";
import
    { a
    , b
    , c
    } from "module2";

let z = 10;

while (!= x.y 0) {
    z = + z 1;
};

if (!= y 0) {
    z = + z 1;
} else {
    z = - z 1;
};

let args = <a, b>
    let compvar = + a b;
</>;

let noargs = <></>;

comp c (1, 2);

export z;
